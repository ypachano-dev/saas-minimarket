Prompt:
Build a modern, highly polished React landing page using Vite, Tailwind CSS, and motion/react for elegant animations. This is the official marketing site for **3Q Solutions**, an AI/software company, promoting its flagship SaaS product **3Q Nexus** (CRM + ERP + AI agents, currently aimed at minimarkets/retail businesses but built to scale to any business). The site must feel premium, confident, and technology-forward — this is the company's front door, treat it accordingly.

The product is real and already built (a working FastAPI + React SaaS). This site is pure marketing: no real backend, no functional forms — every CTA is a visual placeholder for now. Contact details (phone, email, social) are not finalized yet — show clearly-marked placeholders instead of fabricating real-looking ones.

0. Reference Aesthetic

Follow the exact polish level, spacing rhythm, and animation feel of the structure below (originally written for a wellness brand called "mentality") — same fixed glassmorphic navbar, same hero anatomy, same typographic discipline — but every word, color, image, and section must be rebuilt for 3Q Solutions / 3Q Nexus. Do not keep any "mentality" branding, copy, or imagery.

1. Typography & Global CSS (src/index.css)

Import the fonts "Inter" and "Outfit" from Google Fonts.
Set --font-sans to Inter and --font-display to Outfit.
Set --color-brand-green to #9fff00 and --color-bg-base strictly to #EDEEF5.
Ensure the body uses @apply bg-bg-base text-zinc-900 font-sans antialiased; to carry the #EDEEF5 background throughout the entire page.
Primary text color: #1a1a1a. Secondary/muted text color: #8e8e8e. These two are the only text colors used across the whole site — keep that discipline in every section below.

2. Component Structure (src/App.tsx)

Import Navbar, Hero, Features, AgentesIA, Planes, CtaFinal, and Footer.
Return a div containing <Navbar />, then <main> with Hero, Features, AgentesIA, Planes, CtaFinal in that order, then <Footer /> outside of <main>.
Set the wrapper container classes to min-h-screen bg-bg-base selection:bg-brand-green selection:text-black.

3. Navbar Component (src/components/Navbar.tsx)

Give it fixed styling: fixed top-0 left-0 w-full z-50 py-6 md:py-10 bg-gradient-to-b from-[#f1f1f1]/80 to-transparent backdrop-blur-[2px].
Container layout: a 12-column grid (grid-cols-12 max-w-7xl mx-auto).
Left (cols 1-3): a simple geometric icon (e.g. a diamond/hex outline with a centered dot, fill/stroke #1a1a1a — feel free to design something more distinctive than a basic shape, as long as it reads as a clean tech mark, not a literal logo image) beside the brand name "3Q Solutions" using the display font (Outfit).
Center (cols 4-9): desktop-only hidden nav links, small and lowercase: "producto", "agentes ia", "planes", "nosotros".
Right (cols 10-12): a "encuentra ayuda" anchor link, a black rounded button reading "empezar ahora", and an elegant animated hamburger toggle icon for mobile.
Include an AnimatePresence and motion.div drawer that slides down for mobile with the same navigation links plus the CTA button.

4. Hero Component (src/components/Hero.tsx)

Main styling: <section className="relative min-h-[110vh] sm:min-h-[140vh] w-full flex flex-col items-center justify-start overflow-hidden bg-bg-base">

Background Visual Container (image, not video — see note below):
Absolute wrapper: <div className="absolute top-[15vh] sm:top-[20vh] left-0 w-full h-[95vh] sm:h-[120vh] z-0 pointer-events-none">
Inside it, an image of a sleek robot whose head is open to reveal a glowing, lime-green (#9fff00) circuit-board "brain" — this is 3Q Nexus's visual signature: AI made tangible. Render it with <motion.img className="w-full h-full object-cover" />, and layer a looping subtle glow-pulse animation on top (a motion.div with animate={{ opacity: [0.15, 0.35, 0.15] }}, transition repeat: Infinity) plus a gentle scroll-linked parallax (useScroll + useTransform shifting the image a small number of pixels on scroll). Do not use a third-party hotlinked video or image — generate or supply this asset locally and import it as a static file.
Gradient Mask: add a gradient div at the TOP of the wrapper (h-24 sm:h-32 bg-gradient-to-b from-bg-base to-transparent) AND a matching one at the BOTTOM (bg-gradient-to-t from-bg-base to-transparent) so the image blends cleanly into the #EDEEF5 background on both edges, including while it parallax-shifts on scroll.

Hero Content Alignment: Use <div className="max-w-7xl w-full mx-auto px-8 md:px-16 lg:px-20 relative z-10 grid grid-cols-12 gap-x-4 md:gap-x-8">. Place the text in col-span-12 md:col-span-10 md:col-start-2.

Hero Header (motion.h1): Needs a slide-up fade (initial={{ opacity: 0, y: 15 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.8 }}).

Exact text formatting:
[#1a1a1a] 3Q Nexus convierte tus datos en decisiones
[#8e8e8e] con agentes de IA que [eye-pill element] trabajan por ti, todos los días.

For the inline pill element between "que" and "trabajan", create the same visual device as the reference: w-[16px] md:w-[42px] lg:w-[62px] border-[2px] border-[#1a1a1a] rounded-full inline-flex items-center justify-center containing a tiny solid black dot (w-2 h-2) — read it as a stylized "eye"/indicator that the AI is watching/working.

Demo Pill Component (not a real search bar):
Add a delayed slide-up animation (delay: 0.15) under the header text.
Make a custom capsule <div className="bg-white rounded-[6px] border border-black/[0.05] p-1 pl-4 flex items-center shadow-sm">.
Include an <input placeholder="Pide una demo..."> with transparent background so it looks integrated. No real submit logic — visual only.
Trailing action button: <button className="bg-[#1a1a1a] text-white w-9 h-9 rounded-full relative"> containing an SVG arrow icon.

Architectural Edge Anchors:
Absolute middle-right edge: a glassmorphic pill button for language switching reading "es ↔ en".
Absolute bottom-left corner: "2026" in small neat text (#8e8e8e).
Absolute bottom-right corner: "ERP · CRM · IA" in small neat text (#8e8e8e).

Ensure there is no artificial margin/padding interrupting the visual, so it takes the full intended height of the Hero viewport, while the #EDEEF5 background base anchors the rest of the page cleanly above and below it.

5. Features Component (src/components/Features.tsx)

A section (id="features") with a heading like "Un solo lugar para correr todo el negocio." (font-display, #1a1a1a).
Below it, a responsive grid (1 col mobile, 2 col tablet, 3 col desktop) of white rounded cards (rounded-2xl, border border-black/[0.05], shadow-sm, p-6), one per real 3Q Nexus module:
- Caja / POS — "Ventas rápidas, turnos de caja y cierre diario sin fricción."
- Inventario y Almacén — "Recepción de mercancía, auditorías y stock proyectado en tiempo real."
- Desposte y Balanza — "Descompón productos enteros en cortes y pésalos con trazabilidad."
- Cartera y Créditos — "Cuentas por cobrar, historial de pagos y alertas de vencimiento."
- Bancos y Tesorería — "Saldos consolidados y cuentas por pagar bajo control."
- Delivery — "Mapa en vivo, ETA y seguimiento GPS del repartidor."
- Estadísticas Avanzadas — "Indicadores del negocio que se actualizan solos, sin hojas de cálculo."
Each card fades/slides in on scroll (whileInView, viewport once: true) with a staggered delay (index * 0.08s).

6. Agentes IA Component (src/components/AgentesIA.tsx) — "the three queens"

Section (id="agentes") heading: "Tres agentes de IA, listos para reinar tu operación." (font-display).
Three large cards in a responsive grid (1 col mobile, 3 col desktop), one per AI agent. Each agent is visually personified as a friendly **feminine robot mascot wearing a small queen's crown**, lime-green (#9fff00) + dark metallic accent palette, generated as a static image asset per agent (not a generic icon):
- VALE — "Analista de datos" — mascot holds/shows a holographic bar chart. Bullets: "Lee ventas, mermas y stock en tiempo real" / "Entrega 3 a 5 hallazgos concretos por semana" / "Sugiere acciones, no solo números."
- YHORGE — "Cobranza y tesorería" — mascot holds a glowing coin and a small ledger. Bullets: "Prioriza las cuentas por cobrar más urgentes" / "Vigila cuentas por pagar y saldos bancarios" / "Redacta mensajes de cobranza listos para enviar."
- ALO — "Ventas y CRM" — mascot holds a glowing heart-shaped customer icon. Bullets: "Visión 360° de cada cliente" / "Historial de compras, cartera y pedidos en un solo lugar" / "Responde preguntas de ventas al instante."
Each card: white rounded-3xl panel, mascot image (object-contain, centered), name in font-display, role in muted text, bullets below with a small brand-green bullet marker. Cards fade/slide in on scroll, staggered.

7. Planes Component (src/components/Planes.tsx)

Section (id="planes") heading: "Un plan para cada etapa del negocio." (font-display).
Three pricing cards in a responsive grid (1 col mobile, 3 col desktop). The middle card ("Pro") is visually elevated/highlighted (border-2 border-[#1a1a1a], shadow-lg, slightly translated up on desktop).
- Básico — $29/mes (label it "precio de ejemplo" beneath, since pricing isn't finalized) — Agentes IA: "Ninguno" — Módulos: "Caja, Inventario, Cartera básica" — CTA: "Empezar".
- Pro — $79/mes ("precio de ejemplo") — Agentes IA: ALO mascot shown mini (one agent included) — Módulos: "Todo lo de Básico" — CTA: "Empezar" (this is the highlighted card).
- Max — $149/mes ("precio de ejemplo") — Agentes IA: VALE + YHORGE + ALO mascots shown mini (all three) — Módulos: "Todo + Delivery + Estadísticas avanzadas" — CTA: "Hablar con ventas".
Reuse the same agent mascot images from the Agentes IA section as small thumbnails (w-12 h-12) next to each included agent's name. Cards fade/slide in on scroll, staggered.

8. CTA Final + Footer

A wide dark rounded panel (bg-[#1a1a1a], rounded-3xl, centered text, generous padding) with heading "Dale a tu negocio el equipo de IA que nunca duerme." and subtext "Pide una demo y te mostramos a VALE, YHORGE y ALO trabajando con tus propios datos." Below it, a brand-green rounded button: "Pedir demo".

Footer: logo + "3Q Solutions", copyright "© 2026 3Q Solutions. Todos los derechos reservados.", nav links repeating producto/agentes ia/planes, social icons (instagram, linkedin, x) as plain "#" placeholders, and a small muted disclaimer line: "Teléfono, correo y redes sociales se publican aquí en una próxima iteración."

9. General Rules

No backend calls, no real form submissions, no real outbound links anywhere on the page yet — everything is a styled, animated, but non-functional placeholder.
Keep the #1a1a1a / #8e8e8e / #9fff00 / #EDEEF5 palette disciplined across every single section — no new colors introduced.
Every section should use motion/react `whileInView` + `viewport={{ once: true }}` with a slide-up fade and a staggered delay for any repeated card grid, matching the Hero's slide-up fade language so the whole page feels like one consistent system.
